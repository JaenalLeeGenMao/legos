/**
 * This is the entry file of the Direflow setup.
 *
 * You can add any additional functionality here.
 * For example, this is a good place to hook into your
 * Web Component once it's mounted on the DOM.
 *
 * !This file cannot be removed.
 * It can be left blank if not needed.
 */

// import Button from './direflow-components/button';
// import Input from './direflow-components/input';

// Button.then((element) => {
//   /**
//    * Access DOM node when it's mounted
//    */
  // console.log('Web component Button is mounted on the DOM', element);
//   element.addEventListener('event-click', (e) => {
//     console.log(e.type, e.target);
//   })
// });

// Input.then((element) => {
  // console.log('Web component Input is mounted on the DOM', element);
  // element.addEventListener('event-change', (e) => {
  //   console.log(e.type, e.detail.value);
  // })
// });